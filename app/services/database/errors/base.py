"""
Database service error module.

This module defines the `DBServiceError` class, which serves as the base class for all
custom exceptions related to the database service.
"""


class DBServiceError(Exception):
    """Base class for all database service errors."""
